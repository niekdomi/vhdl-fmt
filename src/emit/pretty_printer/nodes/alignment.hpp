#ifndef EMIT_PRETTY_PRINTER_DECL_ALIGNMENTS_HPP
#define EMIT_PRETTY_PRINTER_DECL_ALIGNMENTS_HPP

namespace emit {
/// @brief Named constants for common alignment columns used across the nodes.
struct AlignmentLevel
{
    static constexpr int NAME = 0;
    static constexpr int MODE = 1;
};
} // namespace emit

#endif // EMIT_PRETTY_PRINTER_DECL_ALIGNMENTS_HPP
